import { Button } from '@/component/Button/Button';

function App() {
  return (
    <div>
      <h1>React 앱</h1>
      <Button type="submit">회원가입</Button>
      <Button type="reset">삭제</Button>
    </div>
  );
}

export default App;
