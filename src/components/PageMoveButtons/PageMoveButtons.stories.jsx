import { PageMoveButtons } from './PageMoveButtons';

export default {
  title: 'Components/PageMoveButtons',
  component: PageMoveButtons,
  tags: ['autodocs'],
  args: { ...PageMoveButtons.defaultProps },
};

/* -------------------------------------------------------------------------- */

export const Default = {
  args: {},
};
