import { ProductReviewSection } from './ProductReviewSection';

export default {
  title: 'Components/ProductReviewSection',
  component: ProductReviewSection,
  tags: ['autodocs'],
  args: { ...ProductReviewSection.defaultProps },
};

/* -------------------------------------------------------------------------- */

export const Default = {
  args: {},
};
