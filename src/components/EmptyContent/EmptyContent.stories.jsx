import { EmptyContent } from './EmptyContent';

export default {
  title: 'Components/EmptyContent',
  component: EmptyContent,
  tags: ['autodocs'],
  args: { ...EmptyContent.defaultProps },
};

/* -------------------------------------------------------------------------- */

export const Default = {
  args: {},
};
